<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <title>改變素食菜單</title>
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">素食</a>
        </div>
    </nav>

    <div class="container-fluid">
        <form onsubmit="popup()" action="/Veg_Edit" method="post">
            <p style="margin-top: 10px;">食品名字:</p>
            <input type="text" class="employee_input_field form-control" name="title" placeholder="名字"><br>
            <p>食品描述:</p>
            <textarea class="employee_input_field form-control" name="description" placeholder="描述" style="resize: none;"></textarea><br>
            <input type="submit" class="submit_button form-control" value="加入">
            <a class="form-control" style="text-decoration: none; text-align:center;" href="/EmployeeMain">回去</a>
        </form>
    </div>

    <!-- <div>
        <form action="/Veg_Edit" method="delete">
            <ul>
                <% vegFoods.forEach(function(food) { %>
                    <li>
                        <h3><%= food.Title %></h3>
                        <p><%= food.Description %></p>
                        <input type="submit" class="submit_button_del" id="<%= food._id %>" value="Delete">
                    </li>
                <% }); %>
            </ul>
        </form>
    </div> -->

    <script>
        function popup(){
            window.alert("食品加入成功");
        }

    // document.getElementById('myForm').addEventListener('click', function(event) {
    //   if (event.target.classList.contains('submit_button_del')) {
    //     const id = event.target.getAttribute('id');
    //     console.log('Button with ID', id, 'was clicked.');
    //     // Perform specific actions based on the ID
    //     // You can also use `this` to refer to the clicked button
    //     console.log('Clicked button text:', this.textContent);
    //   }
    // });
    </script>
</body>
</html>
